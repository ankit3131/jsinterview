let arr = [1,3,5,6]
let target = 4;
let output = 2;
let index = 0;
for(let i=0;i<arr.length;i++) {
    if(arr[i] === target) {
    index = i;
    console.log(index);
    }
    // if(arr[i] >= target) {

    // }
}